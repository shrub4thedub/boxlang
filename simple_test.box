[data -c lock]
  package edith
  installed_to /home/shrub/.local/bin/edith
end

[main]
  echo "echo test: ${lock.installed_to}"
  set test_var ${lock.installed_to}
  echo "set result: $test_var"
  delete ${lock.installed_to}?
  echo "delete completed"
end
